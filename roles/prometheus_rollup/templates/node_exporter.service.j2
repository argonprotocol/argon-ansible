[Unit]
Description=Node Exporter

[Service]
User=root
Group=root
ExecStart=/usr/local/bin/node_exporter --web.listen-address="localhost:{{prometheus_rollup_node_export_port}}"

Restart=always

[Install]
WantedBy=multi-user.target
